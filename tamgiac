def numberOneTriangle(num):
    if num < 1 or num > 10: 
        print("Số không hợp lệ!")
    else:
        for i in range(1, num+1):
            for j in range(i):
                print("1", end=" ") 
            print() 